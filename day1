# 基础知识

# 基本的数据类型
# 整数 0xa 十六进制; 0o10 八进制; 0b10 二进制; TODO 这里进制之间的转换在查查
print(0o10)
print(0b10)
# 转换成2进制
print(bin(1))

# bool 布尔类型 True False
# 浮点型 直接用小数点表示就可以了 像 3.14
# 复数 1+2j
# 字符串 使用单引号和双引号的字符的序列 单双三引号(这里面可以实现一些转义字符,这样转移字符就会被保留也就是被实现)可以跨行 可以在其中自由的使用单双引号
print("hello word" "name")
print('hello word' 'name')
print('''hello word  \nname''')

# 在字符串前面加上r或者R表示该字符串不被特殊处理
print(r"hello word \n name")
# 特殊处理 (转义字符) \\ \t \r \n \' \"
# 不用大括号,4个空格缩进(约定俗称)
# 续行 也就是在行尾添加一个\

# 基础语法
# 变量的命名 这里和java差不多
# python 没有没有标识符的常量 像java中的final
# 变量

# python 是动态语言(不用事先生命语言类型) 强类型语言(不同类型是必须先强制转换)

# 运算符 和java类似
# 整除 这个代表的是相应的整数
print(1 // 5)
# 除法 这里的结果是浮点数
print(1 / 5)
# 相应的平方计算 用两个**表示
print(2 ** 2)

# 位运算符
# &  | ~ ^ >> <<
# 32//4 和32>>2 结果是一样的
# 一个有趣的问题 ~12等于多少 -13 TODO 想想是怎么来的 视频51分钟 马哥教育
# 反码 整数讲源码按位取反;负数符号位不变其余按位取反
# 补码 整数的补码是其本身;复数符号位不变其余按位取反后加1


# 比较运算符
# == != > < >= <=
# 两个特殊的比较
# print(1 < '1') 这个是不可以比较的 类型不一样
print(1 == '1')  # 但是这个可以比较 但是返回的结果是False

# 逻辑运算符
# 与或非 and or not
# 短路
# and 如果一个为false 后面就没有必要计算了
# or 如果有一个为true 后面就没有必要计算了

# 赋值运算符
a = min(1, 3)
print(a)
# 成员运算符(谁在谁里面 谁不在谁里面)
# in/not in
# 身份运算符(你是不是这个班的)
# is / is not

# 分支判断
# 单分支语句
# if 语句的写法 主要是后面那个冒号 (这里其实相当于一个代码块  后面是要跟相应的冒号的 )
# 一些常见的另类判断
# "" 假
# "string" 真
# 0 假
# ()空元祖 假
# []空列表 假
# {}空字典 假
# None 假
if 2 > 1:
    print("1真的是大于2的")

# 多分支语句
# if elif elif else
a = 100
if (a < 10):
    print("a 是小于10的")
elif (20 > a > 50):
    print('a 是在20到50之间的')
else:
    print('a 是大于50的')

